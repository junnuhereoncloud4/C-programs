#include<stdio.h>
int main()
{
    int n,fact=1,i;
    printf("enetr n value");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    {
        fact=fact*i;
        printf("factorial of %d=%d",n,fact);
    }
    return 0;
    
}

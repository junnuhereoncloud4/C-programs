#include<stdio.h>
int main()

{
    int i,n,s;
    printf("enter table number and table size");
    scanf("%d%d",&n,&s); 
    for(i=1;i<=s;i++)
    {
        printf("%d*%d=%d\n",n,i,n*i);
    }
    return 0;
}
